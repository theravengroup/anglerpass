"use client";

import { MessagesView } from "@/components/shared/MessagesView";

export default function GuideMessagesPage() {
  return (
    <MessagesView
      accentColor="charcoal"
      subtitle="Communicate with anglers and club managers."
      emptyDescription="Conversations with anglers and clubs will appear here."
    />
  );
}
