import "server-only";

/**
 * Re-export from untyped-admin.ts for backward compatibility.
 * @deprecated Import from `@/lib/supabase/untyped-admin` instead.
 */
export { createCompassAdminClient } from "./untyped-admin";
